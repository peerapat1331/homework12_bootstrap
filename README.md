# homework12_bootstrap
git checkout -b  development
git branch
git add .
git commit -m "เริ่มโปรเจคจากงานเดิม"
git add .
git commit -m "เพิ่มเซิร์ตคำค้นหา"
git add .
git commit -m "เพิ่ม carousel" 
git add .
git commit -m "เพิ่ม gard" 